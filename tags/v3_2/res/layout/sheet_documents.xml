<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
   xmlns:android="http://schemas.android.com/apk/res/android"
   android:layout_weight="1"
   android:id="@+id/sheet_documents"
   android:layout_margin="@dimen/page_margin"
   android:layout_height="fill_parent" android:layout_width="fill_parent" style="@style/BorderPanel">
	<ListView android:layout_width="fill_parent" android:id="@android:id/list"
		android:layout_height="fill_parent"></ListView>

<TextView android:text="Keine Dokumente gefunden." android:id="@+android:id/empty" android:gravity="center" android:layout_height="wrap_content" android:layout_width="wrap_content" android:drawableTop="@drawable/ic_menu_clear_playlist" android:drawablePadding="10dp" android:layout_gravity="center" android:visibility="gone"></TextView>

</FrameLayout>